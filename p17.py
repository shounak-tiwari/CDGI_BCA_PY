# Yuvraj sell plot in x location, he enter the dimension of plot in length and breadth calculate the total area of plot... 

length = float(input("Enter the length "))

breadth = float(input("Enter the breadth "))

area =  length * breadth 

print(area)